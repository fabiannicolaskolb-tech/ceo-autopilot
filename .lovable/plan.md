

## Plan: "Profil" aus der Navigation entfernen

**Änderung in `src/components/ui/floating-header.tsx`:**

Den Eintrag `{ title: 'Profil', url: '/profile', icon: User }` aus dem `navItems`-Array entfernen. Profil bleibt weiterhin über das Avatar-Dropdown (Desktop) und das mobile Menü erreichbar.

